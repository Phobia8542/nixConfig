# Gaming Module

Everything you'll need to game on Linux

Includes **Steam**, Lutris, tools for launch options, and tools for installing and managing proton versions.

Native games are disabled by default. But can easily be enabled by uncommenting `./games.nix` in `default.nix` file.