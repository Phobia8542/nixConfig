# Ollama / LLM Module

This module is for Ollama. Select one based on your GPU. They're configured to use **open-webui**. You can still use the terminal. 

The default port has been changed from `8080` to `9090`, because of tools like `zaproxy`.